name: Release

on:
  push:
    tags:
      - "*"
  workflow_dispatch:

permissions:
  contents: write
  packages: write

jobs:
  reusable:
    uses: groundsgg/.github/.github/workflows/gradle-publish.yml@main
